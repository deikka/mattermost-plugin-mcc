---
phase: 01-foundation-core-plane-commands
plan: 01
type: execute
wave: 1
depends_on: ["01-00"]
files_modified:
  - plugin.json
  - go.mod
  - go.sum
  - Makefile
  - server/plugin.go
  - server/configuration.go
  - server/command.go
  - server/command_router.go
  - server/command_handlers.go
  - server/bot.go
  - server/manifest.go
  - assets/icon.svg
autonomous: true
requirements:
  - CONF-01
  - CONF-04
  - CONF-05

must_haves:
  truths:
    - "Plugin compiles and produces a valid .tar.gz bundle"
    - "Admin sees Plane URL, API Key, and Workspace fields in System Console under the plugin settings"
    - "Plugin creates a bot account named task-bot on activation"
    - "User can type /task and see autocomplete tree with all subcommands and aliases"
    - "User can run /task help and see formatted list of all available commands"
    - "Unknown subcommands return a helpful suggestion message"
  artifacts:
    - path: "plugin.json"
      provides: "Plugin manifest with id, settings_schema (PlaneURL, PlaneAPIKey, PlaneWorkspace), server executables"
      contains: "settings_schema"
    - path: "server/plugin.go"
      provides: "Plugin struct, OnActivate hook, ServeHTTP dispatch"
      exports: ["Plugin", "OnActivate"]
      min_lines: 40
    - path: "server/configuration.go"
      provides: "Thread-safe configuration with RWMutex"
      exports: ["configuration", "getConfiguration", "OnConfigurationChange"]
    - path: "server/command.go"
      provides: "Slash command registration with full AutocompleteData tree"
      exports: ["registerCommands"]
    - path: "server/command_router.go"
      provides: "Command routing via handler map with alias support and suggest-on-unknown"
      exports: ["ExecuteCommand"]
    - path: "server/command_handlers.go"
      provides: "Individual handler functions for help command"
      exports: ["handleHelp"]
    - path: "server/bot.go"
      provides: "Bot account creation and management"
      exports: ["ensureBot"]
  key_links:
    - from: "server/plugin.go"
      to: "server/configuration.go"
      via: "OnActivate calls OnConfigurationChange"
      pattern: "OnConfigurationChange"
    - from: "server/plugin.go"
      to: "server/bot.go"
      via: "OnActivate calls ensureBot"
      pattern: "EnsureBot"
    - from: "server/plugin.go"
      to: "server/command.go"
      via: "OnActivate calls registerCommands"
      pattern: "registerCommands"
    - from: "server/command_router.go"
      to: "server/command_handlers.go"
      via: "Handler map dispatches to handler functions"
      pattern: "commandHandlers"
---

<objective>
Scaffold the Mattermost plugin from the starter template and implement the foundation layer: plugin manifest with System Console settings, bot account, thread-safe configuration, slash command registration with full autocomplete tree (including aliases), command routing with handler map, and the /task help command.

Purpose: Establish the compilable plugin skeleton that all subsequent plans build upon. After this plan, the plugin can be installed in Mattermost, configured via System Console, and responds to /task commands with routing and help output.

Output: A buildable Mattermost Go plugin with System Console config, bot account, command autocomplete, routing, and /task help.
</objective>

<execution_context>
@/Users/alex/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alex/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-core-plane-commands/01-CONTEXT.md
@.planning/phases/01-foundation-core-plane-commands/01-RESEARCH.md
@.planning/phases/01-foundation-core-plane-commands/01-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold plugin from starter template and configure manifest</name>
  <files>plugin.json, go.mod, go.sum, Makefile, assets/icon.svg, server/manifest.go</files>
  <action>
Clone the mattermost-plugin-starter-template into the project root (or initialize equivalent structure). Remove the `webapp/` directory since this is a server-only plugin. Then:

1. **plugin.json**: Set plugin ID to `com.klab.mattermost-command-center`, name to "Mattermost Command Center", description. Add `settings_schema` with three settings for System Console:
   - `PlaneURL` (type: text, display_name: "Plane URL", help_text: "Base URL of your Plane instance (e.g., https://plane.example.com)", required)
   - `PlaneAPIKey` (type: text, display_name: "Plane API Key", help_text: "API key from Plane workspace settings", secret: true, required)
   - `PlaneWorkspace` (type: text, display_name: "Plane Workspace Slug", help_text: "Workspace slug from your Plane URL", required)
   Set `min_server_version` to `9.0.0`. Set `server` executables for linux-amd64, darwin-amd64, darwin-arm64.

2. **go.mod**: Module `github.com/klab/mattermost-plugin-mcc`, Go 1.25. Dependencies: `github.com/mattermost/mattermost/server/public`, `github.com/mattermost/mattermost-plugin-api`, `github.com/gorilla/mux`, `github.com/pkg/errors`. Run `go mod tidy`.

3. **Makefile**: Include targets for `build` (go build plugin server binary + bundle into tar.gz), `test` (go test ./server/... -v -count=1), `check-style` (golangci-lint), `deploy` (upload to Mattermost via API). Base on starter template Makefile.

4. **assets/icon.svg**: Create a simple SVG icon (a task/checkmark icon, minimal).

5. **server/manifest.go**: Auto-generated manifest constants from plugin.json (starter template pattern). Include `const manifestID = "com.klab.mattermost-command-center"`.

NOTE: Use Go workspace-compatible import paths. Do NOT use the deprecated `mattermost-server/v5` or `/v6` module paths.
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/mattermost && go mod tidy && go vet ./server/...</automated>
  </verify>
  <done>plugin.json has settings_schema with 3 Plane fields. go.mod compiles. Makefile has build/test targets. assets/icon.svg exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Plugin struct, OnActivate, configuration, and bot account</name>
  <files>server/plugin.go, server/configuration.go, server/bot.go</files>
  <action>
1. **server/plugin.go**: Define the `Plugin` struct embedding `plugin.MattermostPlugin` with fields:
   - `configurationLock sync.RWMutex`
   - `configuration *configuration`
   - `client *pluginapi.Client`
   - `botUserID string`
   - `router *mux.Router`

   Implement `OnActivate()`:
   - Create `pluginapi.Client` via `pluginapi.NewClient(p.API, p.Driver)`
   - Call `p.OnConfigurationChange()` to load initial config
   - Call `p.ensureBot()` to create/get bot account
   - Call `p.registerCommands()` to register the /task command
   - Initialize `p.router = mux.NewRouter()` and call `p.initAPI()` (stub for now — just create empty initAPI method)
   - Spawn `go p.validatePlaneConnection()` for non-blocking health check

   Implement `ServeHTTP()` that delegates to `p.router`.

   Implement `validatePlaneConnection()` as a goroutine: if PlaneURL is empty, skip. Otherwise make a simple GET to `{PlaneURL}/api/v1/workspaces/{workspace}/projects/` with the API key. If fails, DM the system admin via bot: "Could not connect to Plane at {url}. Please verify your configuration in System Console > Plugins > Mattermost Command Center." Use a 5-second timeout. Non-blocking — log errors but don't fail activation.

   Implement `main()` calling `plugin.ClientMain(&Plugin{})`.

2. **server/configuration.go**: Follow the starter template pattern exactly:
   - `configuration` struct with `PlaneURL`, `PlaneAPIKey`, `PlaneWorkspace` string fields (JSON tags matching plugin.json setting keys)
   - `getConfiguration()` with RLock
   - `setConfiguration()` with Lock (includes clone check)
   - `OnConfigurationChange()` loads config via `p.API.LoadPluginConfiguration`

3. **server/bot.go**:
   - `ensureBot()` method: uses `p.client.Bot.EnsureBot()` with username "task-bot", display name "Task Bot", description "Mattermost Command Center - Task management bot". Stores returned ID in `p.botUserID`.
   - `sendEphemeral(userID, channelID, message string)` helper: creates `model.Post` with `p.botUserID` as UserId and calls `p.API.SendEphemeralPost`.
   - `respondEphemeral(args *model.CommandArgs, message string) *model.CommandResponse` helper: calls sendEphemeral and returns empty CommandResponse.

After implementation, flesh out the Wave 0 test stubs in `server/plugin_test.go`:
   - `TestConfiguration`: Verify config loads PlaneURL, PlaneAPIKey, PlaneWorkspace from mock API
   - `TestOnActivate`: Verify OnActivate calls EnsureBot, RegisterCommand, LoadPluginConfiguration
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/mattermost && go build ./server/... && go test ./server/ -run "TestConfiguration|TestOnActivate" -count=1 -v</automated>
  </verify>
  <done>Plugin struct compiles with all fields. OnActivate creates pluginapi client, loads config, creates bot, registers commands. Configuration is thread-safe with RWMutex. Bot helpers exist for ephemeral messaging. TestConfiguration and TestOnActivate pass.</done>
</task>

<task type="auto">
  <name>Task 3: Implement command registration with autocomplete tree, router with aliases, and /task help handler</name>
  <files>server/command.go, server/command_router.go, server/command_handlers.go</files>
  <action>
1. **server/command.go** — `registerCommands()`:
   Build the full `model.AutocompleteData` tree as specified in CONTEXT.md and RESEARCH.md:

   Root: `/task [command]` — "Task management commands"

   Subcommands:
   - `plane [subcommand]` — "Plane task management"
     - `create [title]` — "Create a new task in Plane" (AddTextArgument for optional quick title)
     - `mine` — "Show your assigned tasks in Plane"
     - `status [project]` — "Show project status summary"
   - `p [subcommand]` — "Plane (alias)" (mirrors plane subcommands with short names)
     - `c [title]` — "Create task (alias)"
     - `m` — "Your tasks (alias)"
     - `s [project]` — "Project status (alias)"
   - `connect` — "Link your Mattermost account with Plane"
   - `obsidian [subcommand]` — "Obsidian integration"
     - `setup` — "Configure Obsidian REST API endpoint"
   - `help` — "Show available commands and usage"

   Register as `model.Command` with Trigger "task", AutoComplete true.

2. **server/command_router.go** — `ExecuteCommand()`:
   Define `CommandHandlerFunc` type: `func(p *Plugin, c *plugin.Context, args *model.CommandArgs, subArgs []string) *model.CommandResponse`

   Build handler map:
   ```
   "plane/create" -> handlePlaneCreate (stub: return "Not yet implemented")
   "plane/mine"   -> handlePlaneMine (stub)
   "plane/status" -> handlePlaneStatus (stub)
   "connect"      -> handleConnect (stub)
   "obsidian/setup" -> handleObsidianSetup (stub)
   "help"         -> handleHelp
   ```

   Build alias map:
   ```
   "p/c" -> "plane/create"
   "p/m" -> "plane/mine"
   "p/s" -> "plane/status"
   ```

   Implement `buildCommandKey(parts []string) string` that joins parts with "/" (skipping the first "/task" element).

   In `ExecuteCommand`: parse args.Command into fields, build key, check aliases, dispatch to handler. If no handler found, call `suggestCommand(command)` which computes Levenshtein-like closest match from known commands and returns ephemeral: "Unknown command '/task {input}'. Did you mean `/task {suggestion}`? Run `/task help` for all commands."

   When no subcommand provided (just "/task"), treat as "/task help".

3. **server/command_handlers.go** — `handleHelp()`:
   Return ephemeral response with formatted help text:
   ```
   **Task Management Commands**

   **Plane**
   - `/task plane create [title]` — Create a new task (alias: `/task p c`)
   - `/task plane mine` — Show your assigned tasks (alias: `/task p m`)
   - `/task plane status [project]` — Show project status (alias: `/task p s`)

   **Configuration**
   - `/task connect` — Link your Mattermost account with Plane
   - `/task obsidian setup` — Configure Obsidian REST API endpoint

   **Other**
   - `/task help` — Show this help message
   ```

   All other handlers: stub implementations returning ephemeral "This command is not yet implemented. Coming in the next update." These stubs will be replaced in Plans 02 and 03.

After implementation, flesh out Wave 0 test stubs in `server/command_test.go`:
   - `TestHelpCommand`: Verify /task help returns formatted command list as ephemeral
   - `TestCommandRouting`: Verify handler map dispatches to correct handler
   - `TestCommandAliases`: Verify p/c -> plane/create, p/m -> plane/mine, p/s -> plane/status
   - `TestUnknownCommandSuggestion`: Verify closest-match suggestion for unknown commands
  </action>
  <verify>
    <automated>cd /Users/alex/dev/klab/mattermost && go build ./server/... && go vet ./server/... && go test ./server/ -run "TestHelpCommand|TestCommandRouting|TestCommandAliases|TestUnknownCommandSuggestion" -count=1 -v</automated>
  </verify>
  <done>All /task subcommands and aliases are registered with autocomplete. Command router dispatches via handler map with alias resolution. Unknown commands get "Did you mean...?" suggestions. /task help returns formatted command list as ephemeral message. Stub handlers exist for all other commands. TestHelpCommand, TestCommandRouting, TestCommandAliases, TestUnknownCommandSuggestion pass.</done>
</task>

</tasks>

<verification>
- `go build ./server/...` compiles without errors
- `go vet ./server/...` passes without warnings
- `go test ./server/ -run "TestConfiguration|TestOnActivate|TestHelpCommand|TestCommandRouting|TestCommandAliases" -count=1` all pass
- plugin.json contains `settings_schema` with PlaneURL, PlaneAPIKey, PlaneWorkspace
- All command handlers are reachable via the handler map (no dead code)
- Alias map correctly maps short forms to full command keys
</verification>

<success_criteria>
- Plugin builds to a valid Go binary
- plugin.json defines 3 System Console settings for Plane configuration (CONF-01)
- Bot account is created via pluginapi on activation (CONF-05)
- /task help returns formatted command list as ephemeral message (CONF-04)
- Full autocomplete tree with aliases works for all subcommands
- Command router dispatches correctly and suggests on unknown commands
- All unimplemented handlers return placeholder ephemeral messages
- Unit tests pass for configuration, activation, help command, routing, and aliases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-core-plane-commands/01-01-SUMMARY.md`
</output>
